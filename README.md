# Project Reset

This workspace has been cleared back to a minimal Next.js App Router baseline.

The previous mockup routes, components, and mock data have been removed so the app can be rebuilt step by step from a clean slate.

## Tech Stack

- Next.js App Router
- React
- TypeScript
- Tailwind CSS

## Current Structure

- `src/app/layout.tsx`
- `src/app/globals.css`
- `src/app/page.tsx`

## Scripts

- `npm run dev` – run dev server
- `npm run build` – production build
- `npm run start` – start production server
- `npm run lint` – eslint
